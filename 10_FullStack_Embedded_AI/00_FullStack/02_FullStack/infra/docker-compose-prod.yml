version: '3.9'

services:
  db:
    image: postgres:13
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: myapp
    volumes:
      - ../database/init.sql:/docker-entrypoint-initdb.d/init.sql
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend_gateway:
    build:
      context: ../backend_gateway
      dockerfile: Dockerfile
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    env_file:
      - ../.env.prod    
    volumes:
      - ../firebase:/app/firebase:ro
      - ../shared_libs:/app/shared_libs
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/myapp
      - FIREBASE_CREDENTIALS=/app/firebase/serviceAccountKey.json
    depends_on:
      - db

  # cv_microservice:
  #   build:
  #     context: ../cv_microservice
  #     dockerfile: Dockerfile
  #   command: python src/consumer.py
  #   volumes:
  #     - ../shared_libs:/app/shared_libs
  #   environment:
  #     - DATABASE_URL=postgresql://postgres:postgres@db:5432/myapp
  #   depends_on:
  #     - db

  # frontend_flutter:
  #   build:
  #     context: ../frontend_flutter
  #     dockerfile: Dockerfile
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     - FLUTTER_ENV=production

  # nginx_proxy:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ../nginx_proxy/nginx.conf:/etc/nginx/nginx.conf
  #     - ../nginx_proxy/certs:/etc/nginx/certs
  #   depends_on:
  #     - frontend_flutter
  #     - backend_gateway

volumes:
  db_data:
